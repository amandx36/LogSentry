global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "server"
    static_configs:
      - targets:
          - localhost:8080

  - job_name: "worker"
    static_configs:
      - targets:
          - localhost:9091